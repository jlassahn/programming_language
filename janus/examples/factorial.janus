
janus 1.0;

import io;

def Factorial(x Int32) -> Int32
{
	if x <= 1
	{
		return 1;
	}
	else
	{
		return Factorial(x-1)*x;
	}
}

def main()
{
	def x Int32 = 5;
	def y = Factorial(x);

	io.Print("Factorial(");
	io.Print(x);
	io.Print(") = ");
	io.Print(y);
	io.Print("`lf`");
}

