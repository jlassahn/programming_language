
janus 1.0;

def no_args() -> Void {

	def x = 3.0 + length("a string")*1.0*10^3 + `x`;

	def n = (x != 3) & x >= 1.0 | true | null !~ null;

	def q = !n & !!!n;

	def f Float64 = sqrt(x)^2;

	return;
}

def length(x String) -> Integer {
	return x.length;
}

def lots_of_args(
	a Integer,
	b array(map(String, String)),
	c ref(ref(slice(UInt8)))) -> function () -> Void
{
	return no_args;
}

