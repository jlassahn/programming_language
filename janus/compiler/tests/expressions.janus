
janus 1.0;

import math;

def NoArgs() {

	def x = 3.0 + Length("a string")*1.0*10^3 + `x`;

	def n = (x != 3) & x >= 1.0 | TRUE | NULL !~ NULL;

	def q = !n & !!!n;

	def f Real64 = math.Sqrt(x)^2;

	return;
}

def Length(x String) -> Int64 {
	return x.Length;
}

def LotsOfArgs(
	a Integer,
	b Array(Map(String, String)),
	c Ref(Ref(MSlice(UInt8)))) -> function () -> Void
{
	return NoArgs;
}

