
janus 1.0;

import io;

def Main()
{
	def lst Int16 = [1, 2, 4, 8, 16];

	for i = lst
	{
		io.Print(i.ToString() + "`lf`");
	}

	for i = 0:10
	{
		io.Print(i.ToString() + "`lf`");
	}

	def i = 0;
	while i < 10
	{
		io.Print(i.ToString() + "`lf`");
		i ++;
	}

	if i >= 10
	{
		io.Print("OK`lf`");
	}
	else if TRUE
	{
		io.Print("Can't happen 1`lf`");
	}
	else
	{
		io.Print("Can't happen 2`lf`");
	}

	i = 0;
	while TRUE
	{
		if i >= 10
		{
			break;
		}
		else if i >= 10
		{
			break 1;
		}

		if i <1
		{
			continue;
		}

		io.Print(i.ToString() + "`lf`");
		i ++;
	}

	goto target;

	label target;
}

